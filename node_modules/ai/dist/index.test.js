"use strict";

// streams/index.test.ts
describe("ai", () => {
  beforeEach(() => {
  });
  describe("not a real test", () => {
    it("exports a default client", () => {
      expect(true).toEqual(true);
    });
  });
});
